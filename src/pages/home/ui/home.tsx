import {Fragment} from "react";
import {Text} from "@/shared/ui/text";

const Home = () => {
    return (
        <Fragment>
            <Text isHeader={true}>
                Hello, this is a Home page
            </Text>
        </Fragment>
    );
};

export default Home;
