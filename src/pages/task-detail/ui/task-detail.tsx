// import {useGetTaskDetailQuery} from "@/features/get-tasks/api/tasks.api.ts";
// import {FC} from "react";


const TaskDetail  = () => {

    // const {id} = props

    // const {data: task} = useGetTaskDetailQuery(id)

    return (
        <div>

        </div>
    );
};

export default TaskDetail;
